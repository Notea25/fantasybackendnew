# AJAX поиск в админке (автодополнение для полей выбора)

## ✅ Что это?

**AJAX поиск** (или автодополнение) — это функция SQLAdmin, которая превращает обычные выпадающие списки с тысячами записей в **умные поля с поиском**.

### До (обычный select):
```
[▼ Выбрать игрока]
  - Player 1
  - Player 2
  - Player 3
  ...
  - Player 9999  ← нужно скроллить 🙁
```

### После (AJAX search):
```
[🔍 Начните вводить имя игрока...]
  
Ввели "Messi":
  - Lionel Messi
  - Messias Junior
  
Выбрали из 2-3 результатов ✅
```

---

## 🛠️ Как это работает?

SQLAdmin отправляет AJAX-запрос на бэкенд при вводе текста в поле и возвращает только подходящие записи.

### Код в админке:

```python
class PlayerMatchStatsAdmin(ModelView, model=PlayerMatchStats):
    # ...
    
    form_ajax_refs = {
        "player_id": {
            "fields": ("name",),      # Поиск по полю name
            "order_by": ("name",),    # Сортировка по name
        },
        "match_id": {
            "fields": ("id",),        # Поиск по ID
            "order_by": ("date",),    # Сортировка по дате
        },
    }
```

---

## 📋 Где добавлено

### ✅ CommercialLeagueAdmin (уже было)
- **tours** — поиск по номеру тура
- **squads** — поиск по названию команды

### ✅ SquadAdmin (добавлено)
- **user_id** — поиск по username
- **fav_team_id** — поиск по названию команды
- **league_id** — поиск по названию лиги

### ✅ SquadTourAdmin (добавлено)
- **squad_id** — поиск по названию команды
- **tour_id** — поиск по номеру тура
- **captain_id** — поиск по имени игрока
- **vice_captain_id** — поиск по имени игрока

### ✅ BoostAdmin (добавлено)
- **squad_id** — поиск по названию команды
- **tour_id** — поиск по номеру тура

### ✅ PlayerMatchStatsAdmin (добавлено)
- **player_id** — поиск по имени игрока
- **match_id** — поиск по ID матча
- **team_id** — поиск по названию команды
- **league_id** — поиск по названию лиги

### ✅ UserLeagueAdmin (добавлено)
- **league_id** — поиск по названию лиги
- **creator_id** — поиск по username
- **tours** — поиск по номеру тура (multiple select)
- **squads** — поиск по названию команды (multiple select)

### ✅ ClubLeagueAdmin (добавлено)
- **league_id** — поиск по названию лиги
- **team_id** — поиск по названию команды

---

## 🎯 Как использовать в админке

### 1. Создание/редактирование записи

При создании или редактировании записи в админке:

1. Вместо обычного dropdown увидите поле с подсказкой **"Type to search..."**
2. Начните вводить текст (например, имя игрока)
3. SQLAdmin автоматически отфильтрует записи
4. Выберите нужную запись из короткого списка

### 2. Пример использования

**Создание PlayerMatchStats:**

1. Открываете форму создания
2. В поле **"Player"** вводите: `"Messi"`
3. Появляется список:
   - Lionel Messi
   - Messias Junior
4. Выбираете нужного игрока одним кликом

**Без AJAX поиска** пришлось бы скроллить список из 1000+ игроков!

---

## ⚙️ Параметры form_ajax_refs

### Структура:

```python
form_ajax_refs = {
    "field_name": {
        "fields": ("search_field_1", "search_field_2"),  # По каким полям искать
        "order_by": ("sort_field",),                     # Сортировка результатов
    },
}
```

### Параметры:

- **`fields`** — кортеж полей модели, по которым будет выполняться поиск
  - Например: `("name",)` — поиск только по name
  - Можно несколько: `("name", "email")` — поиск по name ИЛИ email

- **`order_by`** — кортеж полей для сортировки результатов
  - Например: `("name",)` — сортировка по имени (A-Z)
  - Можно несколько: `("league_id", "name")` — сначала по лиге, потом по имени

### Примеры:

#### 1. Простой поиск по имени
```python
"player_id": {
    "fields": ("name",),
    "order_by": ("name",),
}
```

#### 2. Поиск по нескольким полям
```python
"user_id": {
    "fields": ("username", "email"),  # Ищет в username ИЛИ email
    "order_by": ("username",),
}
```

#### 3. Поиск с сортировкой по дате
```python
"match_id": {
    "fields": ("id",),
    "order_by": ("date",),  # Результаты отсортированы по дате
}
```

---

## 🔧 Как добавить для новой модели

### Пример: добавить AJAX поиск для TourAdmin

```python
class TourAdmin(ModelView, model=Tour):
    column_list = [
        Tour.id,
        Tour.number,
        Tour.league_id,
    ]
    
    # Добавляем AJAX поиск
    form_ajax_refs = {
        "league_id": {
            "fields": ("name",),      # Поиск по названию лиги
            "order_by": ("name",),    # Сортировка по названию
        },
    }
```

### Шаги:

1. Определите, какие поля в вашей админке являются **relationship** (ForeignKey, ManyToMany)
2. Добавьте атрибут `form_ajax_refs`
3. Для каждого поля укажите:
   - `fields` — по каким полям **связанной** модели искать
   - `order_by` — как сортировать результаты

---

## 📊 Производительность

### Преимущества AJAX поиска:

✅ **Не загружает все записи** — только подходящие (10-20 вместо 10000)  
✅ **Быстрее работает** — меньше данных передаётся по сети  
✅ **Удобнее для админа** — не нужно скроллить длинные списки  
✅ **Поддерживает пагинацию** — автоматически подгружает следующие страницы  

### Когда особенно полезно:

- 🎮 Выбор игрока из 1000+ записей
- 📅 Выбор матча из сотен матчей
- 👥 Выбор пользователя из тысяч users
- 🏆 Выбор команды из сотен squads

---

## 🐛 Troubleshooting

### Поиск не работает

**Проблема:** Ввожу текст, но ничего не находит

**Решение:**
1. Проверьте, что `fields` указывает на **существующее** поле модели
2. Убедитесь, что поле **индексировано** в БД (для больших таблиц)
3. Проверьте логи SQLAdmin на наличие ошибок

### Медленный поиск

**Проблема:** Поиск работает, но очень медленно

**Решение:**
1. Добавьте индекс на поле поиска в БД:
   ```python
   name: Mapped[str] = mapped_column(String, index=True)
   ```
2. Ограничьте количество результатов (SQLAdmin делает это автоматически)

### Не все записи отображаются

**Проблема:** Ввожу точное название, но запись не появляется

**Решение:**
- SQLAdmin по умолчанию ограничивает результаты (обычно 10-20)
- Вводите более точное название для фильтрации

---

## 📚 Документация

**SQLAdmin form_ajax_refs:**  
https://aminalaee.dev/sqladmin/configurations/model_view/#form_ajax_refs

**Пример использования:**
```python
form_ajax_refs = {
    "category": {
        "fields": ("name", "slug"),
        "order_by": ("name",),
    },
}
```

---

## ✅ Результат

После применения AJAX поиска **все** поля выбора в админке теперь имеют **умный поиск** вместо длинных выпадающих списков!

Это значительно улучшает UX для администраторов при работе с большими объёмами данных.
