[phases.setup]
nixPkgs = ["python312", "poetry"]

[phases.install]
cmds = ["poetry install --no-dev --no-interaction --no-ansi"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "poetry run alembic upgrade heads && poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
